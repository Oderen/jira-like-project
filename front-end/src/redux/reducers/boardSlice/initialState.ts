import initialData from "../../../initialData";
// import { BoardState } from "../../../types/initialData";

const initialState = {
  currentBoard: null,
  items: initialData,
  isLoading: false,
  error: null,
};

export default initialState;
